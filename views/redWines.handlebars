<main class="main main-cast container">
    <section class="section section-main">
        <div class="row d-flex text-center">
            {{#each data}}
            <div class="col-md-3">
                <a href="/redWines/{{id}}">
                <img src="{{image}}" alt="placeholder image" class="img-fluid home-img">
                <h4>{{wine}}</h4>
                </a>
            </div>
            {{/each}}
        </div>
    </section>
</main>